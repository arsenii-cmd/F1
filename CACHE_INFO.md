# Система кэширования F1 Visualization

## Обзор

Приложение использует многоуровневую систему кэширования для оптимизации производительности и снижения нагрузки на API.

## Уровни кэширования

### 1. Кэш данных в памяти (dataCache)
**Время жизни:** 5 минут

Кэшируются:
- Список гонок (races)
- Таблица пилотов (drivers)
- Таблица конструкторов (constructors)

**Как работает:**
- При первом открытии вкладки данные загружаются из API
- HTML-контент таблицы сохраняется в памяти
- При повторном открытии вкладки данные берутся из кэша (мгновенно)
- Через 5 минут кэш автоматически устаревает и данные загружаются заново

**Преимущества:**
- Мгновенное переключение между вкладками
- Нет повторных запросов к API
- Экономия трафика

### 2. Кэш победителей (winnersCache)
**Удалено** - больше не используется, так как приложение работает только с Jolpica API.

### 3. Service Worker кэш (sw.js)
**Версия:** f1-2026-v5

Кэшируются:
- Статические файлы (HTML, CSS, JS, иконки)
- API-ответы (с приоритетом сети)
- Изображения логотипов команд

**Стратегии:**
- **API-запросы:** Network First (сначала сеть, при ошибке — кэш)
- **script.js, styles.css:** Network First (всегда пытаемся получить свежую версию)
- **Изображения:** Cache First (сначала кэш, при промахе — сеть)

## Управление кэшем

### Автоматическая очистка

Кэш очищается автоматически при:
1. Смене сезона (changeSeason)
2. Нажатии кнопки "Обновить данные"
3. Истечении времени жизни (5 минут)

### Ручная очистка

#### Из консоли браузера:
```javascript
// Очистить все кэши (включая Service Worker)
clearAllCaches()

// Посмотреть состояние кэша
debugCache()

// Очистить только кэш данных
dataCache.races = null;
dataCache.drivers = null;
dataCache.constructors = null;
winnersCache.clear();
```

#### Через UI:
- Нажмите кнопку "Обновить данные" — очистит кэш данных и победителей
- Обновите страницу (Ctrl+R) — очистит кэш в памяти, но не Service Worker
- Жёсткое обновление (Ctrl+Shift+R) — очистит всё

## Отладка

### Проверка состояния кэша
```javascript
debugCache()
```

Выведет:
- Статус кэша для каждой секции (races, drivers, constructors)
- Время с последнего обновления
- Время жизни кэша

## Производительность

### До кэширования:
- Переключение между вкладками: 1-3 секунды (запрос к API)
- Повторное открытие вкладки: 1-3 секунды (новый запрос)
- Нагрузка на API: высокая

### После кэширования:
- Переключение между вкладками: мгновенно (из кэша)
- Повторное открытие вкладки: мгновенно (из кэша)
- Нагрузка на API: минимальная (1 запрос на 5 минут)

## Логирование

Все операции с кэшем логируются в консоль:
- `[CACHE] Используем кэшированные данные о гонках` — данные взяты из кэша
- `[CACHE] Данные о гонках сохранены в кэш` — данные сохранены в кэш
- `[CACHE] Кэш данных очищен при смене сезона` — кэш очищен
- `[REFRESH] Все кэши очищены` — принудительная очистка

## Рекомендации

1. **Для разработки:** Используйте жёсткое обновление (Ctrl+Shift+R) или `clearAllCaches()`
2. **Для пользователей:** Кэш работает автоматически, ничего делать не нужно
3. **При проблемах:** Выполните `clearAllCaches()` в консоли
4. **Для отладки:** Используйте `debugCache()` для проверки состояния

## Технические детали

### Структура dataCache:
```javascript
{
    races: '<html>...</html>',        // HTML-контент таблицы
    drivers: '<html>...</html>',      // HTML-контент таблицы
    constructors: '<html>...</html>', // HTML-контент таблицы
    lastUpdate: {
        races: 1234567890,            // timestamp последнего обновления
        drivers: 1234567890,
        constructors: 1234567890
    }
}
```

### Структура winnersCache:
```javascript
Map {
    'session_key_1' => 'Max Verstappen',
    'session_key_2' => 'Lewis Hamilton',
    'session_key_3' => null  // данные недоступны
}
```

## Обновления

При обновлении версии Service Worker (sw.js):
1. Новая версия устанавливается в фоне
2. Пользователь видит уведомление "🔄 Обновление приложения..."
3. Через 2 секунды страница автоматически перезагружается
4. Старые кэши удаляются автоматически
